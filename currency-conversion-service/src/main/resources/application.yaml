spring:
  application:
    name: currency-conversion
  config:
    import: optional:configserver:http://localhost:8888
  jpa:
    show-sql: false
  datasource:
    url: jdbc:h2:mem:testdb
  h2:
    console:
      enabled: true

server:
  port: 8100